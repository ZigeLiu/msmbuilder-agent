[project]
name = "msm-agent"
version = "0.1.0"
dependencies = [
  "numpy",
  "pyyaml",
  "matplotlib",
  "mdtraj",
  "scipy",
  "openai",
  "gradio"
]

[project.scripts]
msm-agent = "msm_agent.cli:main"